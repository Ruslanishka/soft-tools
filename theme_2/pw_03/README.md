# Анализ данных фермерских хозяйств - Вариант 21

Практическая работа по консолидации и аналитической обработке данных фермерских хозяйств с использованием Docker Compose, Jupyter Notebook и PostgreSQL на Ubuntu 20+.

## 🎯 Цель работы

Рассчитать прогнозируемый доход для каждого фермерского хозяйства на основе консолидации данных из трёх источников:
- **fermers.csv** — данные о фермерских хозяйствах (`farm_id`, `farm_name`, `crop_type`)
- **urozhay.xlsx** — данные об урожае (`farm_id`, `harvest_tonnes`)
- **ceny.json** — данные о ценах на рынке (`crop_type`, `price_per_tonne`)

> Прогнозируемый доход = `Урожай (тонн)` × `Цена за тонну`

## 🏗️ Архитектура

- **Docker Compose v2** для оркестрации контейнеров  
- **Jupyter Lab** для анализа данных и визуализации  
- **PostgreSQL 15** для хранения и обработки данных  
- **Python** с библиотеками: `pandas`, `numpy`, `matplotlib`, `seaborn`, `plotly`, `openpyxl`

## 📋 Требования

- Ubuntu 20+  
- Docker и Docker Compose v2 (см. [dockerhelp.md](dockerhelp.md) для установки)  
- Python 3.8+  
- Свободные порты: `8888` (Jupyter), `5432` (PostgreSQL)

## 🚀 Запуск на Ubuntu 20+

### 1. Подготовка системы

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip (если не установлены)
sudo apt install python3 python3-pip -y

# Установка библиотек для генерации данных
pip3 install pandas numpy openpyxl
```

**Установка Docker:** Если Docker не установлен, следуйте инструкциям в [dockerhelp.md](dockerhelp.md)

### 2. Клонирование проекта

```bash
git clone <repository-url>
cd pw_03
```

### 3. Генерация тестовых данных

```bash
python3 data_generator.py
```

**Если возникает ошибка с папкой 'data':**
```bash
# Создать папку вручную и повторить
mkdir -p data
chmod 755 data
python3 data_generator.py
```

### 4. Запуск Docker Compose

```bash
sudo docker compose up -d
```

Или используйте готовый скрипт:
```bash
chmod +x start.sh
./start.sh
```

### 5. Доступ к сервисам

- **Jupyter Lab**: http://localhost:8888 (без пароля)

### 6. Выполнение анализа

1. Откройте браузер и перейдите на http://localhost:8888
2. Перейдите в папку `work/notebooks`
3. Откройте `farm_income_analysis.ipynb`
4. Выполните все ячейки последовательно (Shift+Enter)

## 📊 Структура проекта

```
programmETL/
├── docker-compose.yml          # Конфигурация Docker Compose
├── Dockerfile                  # Образ Jupyter с библиотеками
├── data_generator.py          # Генератор тестовых данных
├── README.md                  # Документация
├── sql/
│   └── init.sql               # SQL-схема для PostgreSQL
├── notebooks/
│   └── farm_income_analysis.ipynb  # Основной анализ
└── data/
    ├── fermers.csv            # Данные фермерских хозяйств
    ├── urozhay.xlsx           # Данные об урожае
    └── ceny.json              # Данные о ценах на рынке
```

## 📈 Этапы анализа

1. **Загрузка данных** из CSV, Excel и JSON файлов
2. **Предварительная обработка** и аудит данных (info(), isnull(), describe())
3. **Очистка данных** обработка пропусков, приведение к snake_case, контроль типов
4. **Консолидация данных** через pd.merge() по ключам farm_id и crop_type
5. **Feature Engineering** - расчёт predicted_income = harvest_tonnes × price_per_tonne
6. **Аналитическая агрегация** группировка по культурам, расчёт среднего и итогового дохода
7. **Визуализация результатов** с помощью matplotlib, seaborn, plotly
8. **Формирование выводов** выявление самых прибыльных хозяйств и культур

## 📊 Визуализации

- Горизонтальная столбчатая диаграмма: топ-10 хозяйств по доходу
- Boxplot: распределение дохода по типу культуры
- Столбчатая диаграмма: средний доход по культурам
- Интерактивные графики с Plotly (опционально)
- Итоговая сводная таблица с ранжированием

## 🗄️ База данных PostgreSQL

Автоматически создаётся схема с таблицами:

- `farms` — фермерские хозяйства
- `harvest` — урожай по хозяйствам
- `market_prices` — цены на культуры
- `farm_predicted_income` — представление с расчётом прогнозируемого дохода

## 📁 Результаты

Все результаты сохраняются в папке `data/`:
- `consolidated_farm_data.csv` - консолидированные данные
- `farm_income_summary.csv` - итоговая выручка по авиакомпаниям

## 🛠️ Управление контейнерами

```bash
# Запуск сервисов
sudo docker compose up -d

# Просмотр статуса
sudo docker compose ps

# Просмотр логов
sudo docker compose logs -f

# Остановка сервисов
sudo docker compose down

# Полная очистка (включая данные)
sudo docker compose down -v

# Пересборка образов
sudo docker compose build --no-cache
sudo docker compose up -d
```

## 🔧 Подключение к PostgreSQL

```python
import psycopg2
import pandas as pd

# Параметры подключения
conn_params = {
    'host': 'localhost',
    'database': 'farm_analytics',
    'user': 'analyst',
    'password': 'analyst123',
    'port': 5432
}

# Подключение и выполнение запросов
conn = psycopg2.connect(**conn_params)
df = pd.read_sql("SELECT * FROM farm_predicted_income", conn)
```

## 📋 Основные выводы

После выполнения анализа вы получите:
- Рейтинг фермерских хозяйств по прогнозируемому доходу
- Сравнительный анализ прибыльности сельскохозяйственных культур
- Бизнес-инсайты: какие культуры приносят наибольший доход в расчёте на хозяйство
- Рекомендации по оптимизации посевных площадей